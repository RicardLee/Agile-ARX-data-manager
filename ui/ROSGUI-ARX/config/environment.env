# ROSGUI Environment Configuration
# 
# Path prefixes for different project components
# You can modify these paths to match your deployment environment

# Base path for cobot_magic project
# Default: ${HOME} (user home directory)
# Alternative examples:
#   - /opt for system-wide installation
#   - /home/robot for specific robot user
#   - /workspace for containerized environments
COBOT_MAGIC_PREFIX=${HOME}

# Base path for agilex workspace (if different from cobot_magic)
# Default: same as COBOT_MAGIC_PREFIX
AGILEX_WS_PREFIX=${HOME}

# ROS workspace path (if different from default)
# Default: ${HOME}
ROS_WS_PREFIX=${HOME}

# Script execution directory
# Default: current working directory
SCRIPTS_PREFIX=.

# Configuration and data directories
CONFIG_PREFIX=.
DATA_PREFIX=${HOME}/data

# Python executable path (auto-detected)
PYTHON_EXECUTABLE=/home/arx/ARX_Play/mobile_aloha/venv/bin/python

# Debug and logging
DEBUG_MODE=false
LOG_LEVEL=INFO

# System credentials for automated operations
# 注意：仅在安全环境中使用，避免在生产环境中硬编码密码
SUDO_PASSWORD=123
